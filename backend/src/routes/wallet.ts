import { Router } from 'express';

const router = Router();

// Wallet routes temporarily disabled due to TypeScript compilation issues
// TODO: Fix AuthenticatedRequest type compatibility and re-enable routes

export default router; 